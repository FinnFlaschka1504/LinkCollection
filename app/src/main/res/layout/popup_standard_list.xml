<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/popup_standardList_clickable"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:background="@drawable/rounded_background"
    android:clickable="true"
    android:elevation="3dp"
    android:foreground="@drawable/ripple"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/popup_standardList_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginVertical="5dp"
        android:layout_marginStart="13dp"
        android:layout_marginEnd="8dp"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:minHeight="35dp"
        android:textSize="18sp"
        tools:text="Text\nmehr Text" />

    <View
        android:id="@+id/popup_standardList_divider"
        android:layout_width="1dp"
        android:layout_height="25dp"
        android:background="?android:attr/listDivider"
        android:visibility="gone"
        tools:visibility="visible" />

    <LinearLayout
        android:id="@+id/popup_standardList_sub_layout"
        android:layout_width="wrap_content"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/popup_standardList_sub"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:clickable="true"
            android:foreground="@drawable/ripple"
            android:padding="5dp"
            android:tint="@color/colorDrawable"
            android:visibility="gone"
            app:srcCompat="@drawable/ic_arrow_right"
            tools:visibility="visible" />
    </LinearLayout>
</LinearLayout>

